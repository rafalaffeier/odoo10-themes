<odoo>
    <data>
      
        <!-- explicit list view definition -->
        <!--
        <record model="ir.ui.view" id="mps.list">
          <field name="name">mps list</field>
          <field name="model">mps.mps</field>
          <field name="arch" type="xml">
            <tree>
              <field name="name"/>
              <field name="value"/>
              <field name="value2"/>
            </tree>
          </field>
        </record>
        -->

        <!-- actions opening views on models -->
        <!--
        <record model="ir.actions.act_window" id="mps.action_window">
          <field name="name">mps window</field>
          <field name="res_model">mps.mps</field>
          <field name="view_mode">tree,form</field>
        </record>
        -->

        <!-- server action to the one above -->
        <!--
        <record model="ir.actions.server" id="mps.action_server">
          <field name="name">mps server</field>
          <field name="model_id" ref="model_mps_mps"/>
          <field name="code">
            action = {
              "type": "ir.actions.act_window",
              "view_mode": "tree,form",
              "res_model": self._name,
            }
          </field>
        </record>
        -->

        <!-- Top menu item -->
        <!--
        <menuitem name="mps" id="mps.menu_root"/>
        -->
        <!-- menu categories -->
        <!--
        <menuitem name="Menu 1" id="mps.menu_1" parent="mps.menu_root"/>
        <menuitem name="Menu 2" id="mps.menu_2" parent="mps.menu_root"/>
        -->
        <!-- actions -->
        <!--
        <menuitem name="List" id="mps.menu_1_list" parent="mps.menu_1"
                  action="mps.action_window"/>
        <menuitem name="Server to list" id="mps" parent="mps.menu_2"
                  action="mps.action_server"/>
        -->
    </data>
</odoo>